const fs = require("fs"); let code = fs.readFileSync("../frontend/services/api.js", "utf8"); code = code.replace(/if \(!isValidPassword\).*(\n\s*if \(!user\.email_verified\) \{[^}]+}\s*){2,}/gm, "if (!isValidPassword) throw new Error('Invalid credentials');\n\n      if (!user.email_verified) {\n        throw new Error('Your account is not verified. Please verify your email first.');\n      }\n"); fs.writeFileSync("../frontend/services/api.js", code);
